# Student Grading System with Encouraging Messages

A simple Python program that takes student marks as input, validates them, assigns a grade, and displays an encouraging message!

##  Features

-  Takes marks input from the user
-  Validates input (only accepts marks 0–100)
-  Computes grade using `if-elif-else` logic
-  Displays an encouraging message for each grade
-  Uses a `while` loop for continuous validation
-  Uses a function to calculate grade and message

##  Grading Logic

| Marks        | Grade | Encouraging Message |
|--------------|-------|----------------------|
| 90–100       | A     | Excellent work! 🎉 |
| 80–89        | B     | Great job! 👍 |
| 70–79        | C     | Good effort! 😊 |
| 60–69        | D     | Stay focused! 💪 |
| < 60         | F     | Keep trying! 🌟 |

## 🖥️ How to Run

1. Make sure Python 3 is installed.
2. Save the code in a file named `grading_system.py`.
3. Run the program using:
